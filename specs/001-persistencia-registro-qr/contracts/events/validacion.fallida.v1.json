{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://aeropass/events/validacion.fallida/v1",
  "title": "validacion.fallida v1",
  "description": "Contract defined in this repo (constitution, Observer pattern) and PRODUCED by the checkpoint module (out of scope for feature 001). Published via QStash when a CredencialAcceso is rejected at the checkpoint. At-least-once delivery: consumers (audit, escalation) must deduplicate by `id`. Contains no document data or images.",
  "type": "object",
  "additionalProperties": false,
  "required": ["id", "tipo", "version", "ocurrido_at", "datos"],
  "properties": {
    "id": { "type": "string", "format": "uuid", "description": "Unique event id" },
    "tipo": { "const": "validacion.fallida" },
    "version": { "const": 1 },
    "ocurrido_at": { "type": "string", "format": "date-time" },
    "datos": {
      "type": "object",
      "additionalProperties": false,
      "required": ["credencial_id", "motivo", "checkpoint_id"],
      "properties": {
        "credencial_id": {
          "type": ["string", "null"],
          "format": "uuid",
          "description": "Credential jti; null if the token could not be decoded"
        },
        "pasajero_id": { "type": ["string", "null"], "format": "uuid" },
        "codigo_vuelo": { "type": ["string", "null"] },
        "motivo": {
          "type": "string",
          "enum": [
            "TOKEN_INVALIDO",
            "FIRMA_INVALIDA",
            "EXPIRADA",
            "REVOCADA",
            "YA_CONSUMIDA",
            "VUELO_CANCELADO",
            "LIMITE_EXCEDIDO",
            "NO_ENCONTRADA"
          ],
          "description": "Link of the validation chain that rejected it (RN-06 → YA_CONSUMIDA, RN-07 → VUELO_CANCELADO)"
        },
        "checkpoint_id": { "type": "string", "description": "Checkpoint identifier" },
        "modo": { "type": "string", "enum": ["EN_LINEA", "CONTINGENCIA"], "default": "EN_LINEA" }
      }
    }
  },
  "examples": [
    {
      "id": "01925f40-1111-7000-8000-00000000000a",
      "tipo": "validacion.fallida",
      "version": 1,
      "ocurrido_at": "2026-09-23T15:10:00Z",
      "datos": {
        "credencial_id": "01925f3f-0000-7000-8000-000000000004",
        "pasajero_id": "01925f39-0000-7000-8000-000000000001",
        "codigo_vuelo": "AV9380",
        "motivo": "YA_CONSUMIDA",
        "checkpoint_id": "BOG-T1-FILTRO-03",
        "modo": "EN_LINEA"
      }
    }
  ]
}
